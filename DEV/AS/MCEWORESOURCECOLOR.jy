if (mbo is not None):
	if (mbo.isNull("schedstart")):
		if (mbo.isNull("targstartdate")): 
			if (mbo.isNull("owner")):
				result = "#ffcb7d";
			else:
				result = "#FF9900";
		else:
			if (mbo.isNull("owner")):
				result = "#e9e9e9";
			else:
				result = "#CCCCCC";
	else:
		if (mbo.isNull("owner")):
			result = "#b3e5b8";
		else:
			result = "#009900";